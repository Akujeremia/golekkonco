<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TemanGalaxy - Jelajahi Alam Semesta Pertemanan</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', 'Segoe UI', sans-serif;
        }
        
        :root {
            --primary: #6c5ce7;
            --secondary: #a29bfe;
            --accent: #fd79a8;
            --success: #00b894;
            --warning: #fdcb6e;
            --text: #dfe6e9;
            --bg-dark: #0f0c29;
            --bg-medium: #302b63;
            --bg-light: #24243e;
            --card-bg: rgba(16, 14, 56, 0.8);
        }
        
        body {
            background: linear-gradient(135deg, var(--bg-dark) 0%, var(--bg-medium) 50%, var(--bg-light) 100%);
            color: var(--text);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 1rem;
            position: relative;
            overflow-x: hidden;
        }
        
        /* Animasi Background */
        .cosmic-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            background: 
                radial-gradient(circle at 20% 80%, rgba(253, 121, 168, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(108, 92, 231, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 40% 40%, rgba(116, 185, 255, 0.05) 0%, transparent 50%);
            animation: cosmicFloat 20s infinite linear;
        }
        
        @keyframes cosmicFloat {
            0% { transform: translate(0, 0) rotate(0deg); }
            100% { transform: translate(-50px, -50px) rotate(360deg); }
        }
        
        .stars {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }
        
        .star {
            position: absolute;
            background-color: white;
            border-radius: 50%;
            animation: twinkle 5s infinite;
        }
        
        @keyframes twinkle {
            0%, 100% { opacity: 0.2; transform: scale(1); }
            50% { opacity: 1; transform: scale(1.2); }
        }
        
        .shooting-star {
            position: absolute;
            width: 100px;
            height: 2px;
            background: linear-gradient(90deg, transparent, white, transparent);
            animation: shoot 8s infinite;
        }
        
        @keyframes shoot {
            0% { transform: translateX(-100px) translateY(0) rotate(45deg); opacity: 0; }
            10% { opacity: 1; }
            100% { transform: translateX(100vw) translateY(100vh) rotate(45deg); opacity: 0; }
        }
        
        .container {
            max-width: 1200px;
            width: 100%;
            background: var(--card-bg);
            backdrop-filter: blur(15px);
            border-radius: 25px;
            padding: clamp(1.5rem, 5vw, 3rem);
            box-shadow: 
                0 20px 40px rgba(0, 0, 0, 0.3),
                0 0 0 1px rgba(255, 255, 255, 0.05),
                inset 0 0 50px rgba(108, 92, 231, 0.1);
            position: relative;
            z-index: 1;
            margin: 1rem;
            overflow: hidden;
        }
        
        .container::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(108, 92, 231, 0.1) 0%, transparent 70%);
            animation: rotate 20s linear infinite;
            z-index: -1;
        }
        
        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .logo {
            font-size: clamp(2.5rem, 8vw, 4rem);
            margin-bottom: 1rem;
            background: linear-gradient(45deg, #ff6b6b, #feca57, #48dbfb, #ff9ff3);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: drop-shadow(0 0 10px rgba(255, 107, 107, 0.3));
            animation: logoGlow 3s ease-in-out infinite alternate;
        }
        
        @keyframes logoGlow {
            0% { filter: drop-shadow(0 0 10px rgba(255, 107, 107, 0.3)); }
            100% { filter: drop-shadow(0 0 20px rgba(254, 202, 87, 0.5)); }
        }
        
        h1 {
            font-size: clamp(2rem, 6vw, 3rem);
            margin-bottom: 1.5rem;
            background: linear-gradient(45deg, #a29bfe, #74b9ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            line-height: 1.3;
            font-weight: 700;
            text-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }
        
        .subtitle {
            font-size: clamp(1.1rem, 4vw, 1.4rem);
            margin-bottom: 1rem;
            color: var(--secondary);
            font-weight: 300;
            letter-spacing: 1px;
        }
        
        p {
            font-size: clamp(1rem, 4vw, 1.2rem);
            margin-bottom: 2.5rem;
            line-height: 1.7;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
            color: var(--text);
            font-weight: 300;
        }
        
        .interactive-section {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 20px;
            padding: 2rem;
            margin: 2rem 0;
            border: 1px solid rgba(255, 255, 255, 0.1);
            position: relative;
            overflow: hidden;
        }
        
        .interactive-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.05), transparent);
            transition: left 0.5s;
        }
        
        .interactive-section:hover::before {
            left: 100%;
        }
        
        .highlight {
            background: linear-gradient(45deg, rgba(116, 185, 255, 0.2), rgba(253, 121, 168, 0.2));
            padding: clamp(0.8rem, 2vw, 1rem) clamp(1.2rem, 3vw, 1.8rem);
            border-radius: 15px;
            display: inline-flex;
            align-items: center;
            margin: 1.5rem 0;
            border: 1px solid rgba(116, 185, 255, 0.3);
            animation: pulse 2s infinite, float 6s ease-in-out infinite;
            font-size: clamp(0.9rem, 3vw, 1.1rem);
            max-width: 90%;
            gap: 10px;
            backdrop-filter: blur(5px);
        }
        
        .link-button {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            color: white;
            text-decoration: none;
            font-size: clamp(1.1rem, 4vw, 1.4rem);
            font-weight: 700;
            padding: clamp(1rem, 3vw, 1.2rem) clamp(1.8rem, 5vw, 2.5rem);
            border-radius: 50px;
            box-shadow: 
                0 10px 25px rgba(108, 92, 231, 0.4),
                0 0 0 0 rgba(108, 92, 231, 0.5);
            transition: all 0.4s ease;
            margin: 1.5rem 0;
            border: none;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            width: 100%;
            max-width: 450px;
            animation: buttonPulse 2s infinite;
        }
        
        @keyframes buttonPulse {
            0% { box-shadow: 0 10px 25px rgba(108, 92, 231, 0.4), 0 0 0 0 rgba(108, 92, 231, 0.5); }
            70% { box-shadow: 0 10px 25px rgba(108, 92, 231, 0.4), 0 0 0 15px rgba(108, 92, 231, 0); }
            100% { box-shadow: 0 10px 25px rgba(108, 92, 231, 0.4), 0 0 0 0 rgba(108, 92, 231, 0); }
        }
        
        .link-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: 0.5s;
        }
        
        .link-button:hover {
            transform: translateY(-8px) scale(1.05);
            box-shadow: 
                0 15px 30px rgba(108, 92, 231, 0.6),
                0 0 0 0 rgba(108, 92, 231, 0.5);
            animation: none;
        }
        
        .link-button:hover::before {
            left: 100%;
        }
        
        .link-button:active {
            transform: translateY(-2px) scale(1.02);
        }
        
        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }
        
        .feature {
            background: linear-gradient(145deg, rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.05));
            padding: clamp(1.5rem, 3vw, 2rem);
            border-radius: 20px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.4s ease;
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
            overflow: hidden;
        }
        
        .feature::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, var(--accent), var(--primary));
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.4s ease;
        }
        
        .feature:hover {
            transform: translateY(-15px) scale(1.03);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
            background: linear-gradient(145deg, rgba(255, 255, 255, 0.15), rgba(255, 255, 255, 0.08));
        }
        
        .feature:hover::before {
            transform: scaleX(1);
        }
        
        .feature i {
            font-size: clamp(2.5rem, 6vw, 3.5rem);
            margin-bottom: 1.5rem;
            background: linear-gradient(45deg, var(--accent), var(--primary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: drop-shadow(0 5px 10px rgba(253, 121, 168, 0.3));
        }
        
        .feature h3 {
            font-size: clamp(1.3rem, 4vw, 1.6rem);
            margin-bottom: 1rem;
            color: #74b9ff;
            font-weight: 600;
        }
        
        .feature p {
            font-size: clamp(0.95rem, 3vw, 1.1rem);
            margin-bottom: 0;
            color: var(--text);
            font-weight: 300;
        }
        
        .floating-elements {
            position: fixed;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }
        
        .floating-element {
            position: absolute;
            border-radius: 50%;
            opacity: 0.1;
            animation: floatAround 15s infinite linear;
        }
        
        @keyframes floatAround {
            0% { transform: translate(0, 0) rotate(0deg); }
            33% { transform: translate(30px, 50px) rotate(120deg); }
            66% { transform: translate(-20px, 80px) rotate(240deg); }
            100% { transform: translate(0, 0) rotate(360deg); }
        }
        
        .user-stats {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin: 2rem 0;
            flex-wrap: wrap;
        }
        
        .stat {
            background: rgba(255, 255, 255, 0.05);
            padding: 1rem 1.5rem;
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            min-width: 150px;
        }
        
        .stat-number {
            font-size: 2rem;
            font-weight: 700;
            background: linear-gradient(45deg, var(--accent), var(--primary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .stat-label {
            font-size: 0.9rem;
            color: var(--secondary);
            margin-top: 0.5rem;
        }
        
        .notification {
            position: fixed;
            top: 20px;
            right: 20px;
            background: var(--success);
            color: white;
            padding: 1rem 1.5rem;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            transform: translateX(150%);
            transition: transform 0.5s ease;
            z-index: 1000;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .notification.show {
            transform: translateX(0);
        }
        
        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                margin: 0.5rem;
                padding: 1.5rem 1rem;
                border-radius: 20px;
            }
            
            .features {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
            
            .feature {
                padding: 1.5rem;
            }
            
            .user-stats {
                gap: 1rem;
            }
            
            .stat {
                min-width: 120px;
                padding: 0.8rem 1rem;
            }
            
            .interactive-section {
                padding: 1.5rem;
            }
        }
        
        @media (max-width: 480px) {
            .container {
                padding: 1.2rem 0.8rem;
            }
            
            .feature {
                padding: 1.2rem;
            }
            
            .highlight {
                margin: 1rem 0;
                padding: 0.8rem 1rem;
                flex-direction: column;
                text-align: center;
            }
            
            .user-stats {
                flex-direction: column;
                align-items: center;
            }
            
            .stat {
                width: 100%;
                max-width: 200px;
            }
        }
    </style>
</head>
<body>
    <div class="cosmic-bg"></div>
    <div class="stars" id="stars"></div>
    <div class="floating-elements" id="floatingElements"></div>
    <div class="shooting-star" style="top: 20%; animation-delay: 2s;"></div>
    <div class="shooting-star" style="top: 60%; animation-delay: 5s;"></div>
    <div class="shooting-star" style="top: 80%; animation-delay: 7s;"></div>
    
    <div class="notification" id="notification">
        <i class="fas fa-check-circle"></i>
        <span>Selamat! Anda akan segera menemukan teman baru!</span>
    </div>
    
    <div class="container">
        <div class="logo">
            <i class="fas fa-globe-americas"></i>
        </div>
        
        <h1>Jelajahi Galaksi Pertemanan</h1>
        <p class="subtitle">Temukan Koneksi Baru di Alam Semesta Sosial</p>
        
        <p>Setiap bintang di galaksi ini mewakili seseorang yang menunggu untuk terhubung denganmu. Mari jelajahi bersama dan temukan persahabatan yang tak terduga!</p>
        
        <div class="user-stats">
            <div class="stat">
                <div class="stat-number" id="userCount">10.247</div>
                <div class="stat-label">Pengguna Aktif</div>
            </div>
            <div class="stat">
                <div class="stat-number" id="matchCount">3.892</div>
                <div class="stat-label">Pertemanan Terjalin</div>
            </div>
            <div class="stat">
                <div class="stat-number" id="countryCount">127</div>
                <div class="stat-label">Negara Terjangkau</div>
            </div>
        </div>
        
        <div class="interactive-section">
            <div class="highlight">
                <i class="fas fa-rocket"></i>
                <span>Bersiaplah untuk petualangan sosial terbesarmu!</span>
            </div>
            
            <button class="link-button" id="mainButton">
                <i class="fas fa-meteor"></i>
                <span>JELAJAHI GALAKSI PERTEMANAN</span>
                <i class="fas fa-external-link-alt"></i>
            </button>
            
            <p style="font-size: 0.9rem; margin-top: 1rem; opacity: 0.8;">
                Klik untuk membuka dunia pertemanan tanpa batas
            </p>
        </div>
        
        <div class="features">
            <div class="feature">
                <i class="fas fa-satellite-dish"></i>
                <h3>Koneksi Antar Bintang</h3>
                <p>Temukan teman dari berbagai belahan galaksi dengan minat dan passion yang sama</p>
            </div>
            
            <div class="feature">
                <i class="fas fa-user-astronaut"></i>
                <h3>Profil Intergalaksi</h3>
                <p>Buat profil unik yang mencerminkan kepribadianmu di alam semesta sosial</p>
            </div>
            
            <div class="feature">
                <i class="fas fa-comment-dots"></i>
                <h3>Komunikasi Real-time</h3>
                <p>Terhubung secara instan dengan teknologi komunikasi tercanggih di galaksi</p>
            </div>
        </div>
    </div>

    <script>
        // Inisialisasi elemen visual
        function initVisuals() {
            createStars();
            createFloatingElements();
            animateStats();
        }
        
        // Membuat efek bintang
        function createStars() {
            const starsContainer = document.getElementById('stars');
            const starCount = window.innerWidth < 768 ? 100 : 200;
            
            for (let i = 0; i < starCount; i++) {
                const star = document.createElement('div');
                star.classList.add('star');
                
                const x = Math.random() * 100;
                const y = Math.random() * 100;
                const size = window.innerWidth < 768 ? 
                    Math.random() * 2 : Math.random() * 3;
                const duration = 3 + Math.random() * 7;
                const delay = Math.random() * 5;
                
                star.style.left = `${x}%`;
                star.style.top = `${y}%`;
                star.style.width = `${size}px`;
                star.style.height = `${size}px`;
                star.style.animationDuration = `${duration}s`;
                star.style.animationDelay = `${delay}s`;
                
                starsContainer.appendChild(star);
            }
        }
        
        // Membuat elemen mengambang
        function createFloatingElements() {
            const container = document.getElementById('floatingElements');
            const elementCount = 8;
            
            for (let i = 0; i < elementCount; i++) {
                const element = document.createElement('div');
                element.classList.add('floating-element');
                
                const size = 20 + Math.random() * 60;
                const colors = ['#6c5ce7', '#fd79a8', '#74b9ff', '#00b894', '#fdcb6e'];
                const color = colors[Math.floor(Math.random() * colors.length)];
                
                element.style.width = `${size}px`;
                element.style.height = `${size}px`;
                element.style.background = `radial-gradient(circle, ${color}, transparent)`;
                element.style.top = `${Math.random() * 100}%`;
                element.style.left = `${Math.random() * 100}%`;
                element.style.animationDuration = `${15 + Math.random() * 20}s`;
                element.style.animationDelay = `${Math.random() * 5}s`;
                
                container.appendChild(element);
            }
        }
        
        // Animasi statistik
        function animateStats() {
            const userCount = document.getElementById('userCount');
            const matchCount = document.getElementById('matchCount');
            const countryCount = document.getElementById('countryCount');
            
            animateValue(userCount, 0, 10247, 2000);
            animateValue(matchCount, 0, 3892, 2000);
            animateValue(countryCount, 0, 127, 2000);
        }
        
        function animateValue(element, start, end, duration) {
            let startTimestamp = null;
            const step = (timestamp) => {
                if (!startTimestamp) startTimestamp = timestamp;
                const progress = Math.min((timestamp - startTimestamp) / duration, 1);
                const value = Math.floor(progress * (end - start) + start);
                element.textContent = value.toLocaleString();
                if (progress < 1) {
                    window.requestAnimationFrame(step);
                }
            };
            window.requestAnimationFrame(step);
        }
        
        // Notifikasi
        function showNotification() {
            const notification = document.getElementById('notification');
            notification.classList.add('show');
            
            setTimeout(() => {
                notification.classList.remove('show');
            }, 4000);
        }
        
        // Efek klik tombol
        document.getElementById('mainButton').addEventListener('click', function() {
            // Efek visual
            this.style.background = 'linear-gradient(45deg, #00b894, #00cec9)';
            this.innerHTML = '<i class="fas fa-spinner fa-spin"></i><span>MEMUAT GALAKSI...</span>';
            
            // Tampilkan notifikasi
            showNotification();
            
            // Redirect setelah delay
            setTimeout(() => {
                window.open('', '_blank');
                
                // Reset tombol
                setTimeout(() => {
                    this.style.background = '';
                    this.innerHTML = '<i class="fas fa-meteor"></i><span>JELAJAHI GALAKSI PERTEMANAN</span><i class="fas fa-external-link-alt"></i>';
                }, 1000);
            }, 1500);
        });
        
        // Inisialisasi saat halaman dimuat
        window.addEventListener('load', initVisuals);
        
        // Recreate visuals on resize
        window.addEventListener('resize', function() {
            document.getElementById('stars').innerHTML = '';
            document.getElementById('floatingElements').innerHTML = '';
            initVisuals();
        });
        
        // Efek parallax sederhana
        window.addEventListener('mousemove', function(e) {
            const x = e.clientX / window.innerWidth;
            const y = e.clientY / window.innerHeight;
            
            document.querySelector('.cosmic-bg').style.transform = `translate(${x * 20}px, ${y * 20}px)`;
        });
    </script>
</body>
</html>
